<!DOCTYPE html>
<html>
<head>
    <title>Pulsy - Your AI Advisor</title>
    <style>
        body {
            font-family: "Aeonik", -apple-system, BlinkMacSystemFont, sans-serif;
            margin: 0;
            padding: 0;
            height: 100vh;
            overflow: auto;
            color: #1a1a1a;
            background: #E8E1D7;
        }

        .video-background {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            overflow: hidden;
        }

        .video-background video {
            min-width: 100%;
            min-height: 100%;
            object-fit: cover;
            filter: brightness(50%);
        }

        .login-container {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            position: relative;
            z-index: 1;
            padding: 2rem 0;
        }

        .login-box {
            background: #F5F1EB;
            padding: 3rem;
            border-radius: 12px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
            width: 100%;
            max-width: 400px;
            backdrop-filter: blur(10px);
            border: 1px solid #D4CEC4;
        }

        .app-title {
            text-align: center;
            color: #0B365B;
            font-size: 2.5rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .app-subtitle {
            text-align: center;
            color: #0B365B;
            font-size: 1.1rem;
            margin-bottom: 2.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            margin-bottom: 0.5rem;
            color: #0B365B;
            font-weight: 500;
        }

        input, select {
            width: 100%;
            padding: 1rem;
            border: 2px solid #D4CEC4;
            border-radius: 8px;
            font-size: 1rem;
            background: #F5F1EB;
            transition: all 0.2s ease;
            box-sizing: border-box;
            color: #1a1a1a;
        }

        input:focus, select:focus {
            outline: none;
            border-color: #0B365B;
        }

        .login-button, .signup-button {
            width: 100%;
            padding: 1rem;
            background-color: #0B365B;
            color: #E8E1D7;
            border: 2px solid #0B365B;
            border-radius: 30px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            margin-top: 1rem;
        }

        .login-button:hover, .signup-button:hover {
            background-color: transparent;
            color: #0B365B;
        }

        .toggle-form {
            text-align: center;
            margin-top: 1rem;
            color: #0B365B;
            cursor: pointer;
        }

        .error-message {
            color: #ff3b30;
            margin-bottom: 1rem;
            text-align: center;
            min-height: 20px;
            font-size: 0.9rem;
        }

        .success-message {
            color: #34C759;
            margin-bottom: 1rem;
            text-align: center;
            min-height: 20px;
            font-size: 0.9rem;
            display: none;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .login-box {
            animation: fadeIn 0.3s ease;
        }

        #signupForm {
            display: none;
        }

        textarea {
            width: 100%;
            padding: 1rem;
            border: 2px solid #D4CEC4;
            border-radius: 8px;
            font-size: 1rem;
            background: #F5F1EB;
            transition: all 0.2s ease;
            box-sizing: border-box;
            color: #1a1a1a;
            min-height: 100px;
            resize: vertical;
        }

        textarea:focus {
            outline: none;
            border-color: #0B365B;
        }

        .checkmark {
            color: #34C759;
            font-size: 2rem;
            display: none;
            text-align: center;
            margin: 1rem 0;
        }
    </style>
</head>
<body>
    <div class="video-background">
        <video autoplay muted loop>
            <source src="assets/fitness-background.mp4" type="video/mp4">
        </video>
    </div>
    <div class="login-container">
        <div class="login-box">
            <h1 class="app-title">PULSY</h1>
            <p class="app-subtitle">Your AI Wearable Device Advisor</p>
            
            <form id="loginForm" action="/chat/home" method="GET">
                <div class="form-group">
                    <label for="username">Username</label>
                    <input type="text" id="username" name="username" required>
                </div>
                <div class="form-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required>
                </div>
                <div class="error-message" id="errorMessage">
                    <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
                        <%= errorMessage %>
                    <% } %>
                </div>
                <button type="submit" class="login-button">Login</button>
            </form>


            <div style="text-align: center; margin-top: 1rem;">
                <a href="/login/signup" style="color: #0B365B; text-decoration: none; cursor: pointer;">Create a New Account</a>
            </div>
        </div>
    </div>
</body>
</html>
