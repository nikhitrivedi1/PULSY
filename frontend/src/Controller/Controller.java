package Controller;

/**
 * Controller interface
 */
public interface Controller {

  /**
   * method for calling a specific GUI action to take place
   * Example: Controller for mainPage will call go() for methods in the view to display GUI elements
   */
  void go();
}
