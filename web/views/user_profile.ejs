<!DOCTYPE html>
<html>
<head>
    <title>Agentic Preferences</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
        :root {
            --main-bg: #E8E1D7;
            --card-bg: #F5F1EB;
            --primary: #0B365B;
            --secondary: #1a1a1a;
            --success: #2ecc71;
            --danger: #e74c3c;
            --border-light: #D4CEC4;
            --shadow: 0 2px 16px 0 rgba(11,54,91,0.06), 0 3px 24px 2px rgba(11,54,91,0.04);
        }

        html, body {
            height: 100%;
        }

        body {
            font-family: "Aeonik", -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--main-bg);
            color: var(--secondary);
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }

        .container {
            margin: 0 auto;
            margin-top: 70px;
            max-width: 430px;
            padding: 0 16px 60px 16px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .profile-header {
            width: 100%;
            margin-bottom: 26px;
            text-align: left;
        }

        .profile-header h1 {
            color: var(--primary);
            font-size: 2.3rem;
            font-weight: 700;
            margin: 0;
            letter-spacing: -0.5px;
        }

        @media(max-width:600px){
            .container { margin-top: 36px; padding: 0 7vw;}
            .profile-header h1 {font-size:1.6rem;}
        }

        .preferences-card {
            width: 100%;
            background: var(--card-bg);
            box-shadow: var(--shadow);
            border-radius: 26px;
            padding: 36px 30px 30px 30px;
            margin-bottom: 22px;
            display: flex;
            flex-direction: column;
            align-items: stretch;
            border: 1.2px solid #f1ece6;
        }

        .preferences-list {
            padding: 0;
            margin: 0;
            list-style: none;
        }

        .preference-bar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            background: #fff;
            border-radius: 22px;
            margin-bottom: 16px;
            box-shadow: 0 2px 12px rgba(11,54,91,0.03);
            padding: 18px 26px 17px 20px;
            border: 1.5px solid var(--border-light);
            transition: box-shadow 0.18s;
            position: relative;
        }

        .preference-bar:last-child {
            margin-bottom: 0;
        }

        .preference-name {
            font-size: 1.08rem;
            font-weight: 500;
            color: var(--primary);
            letter-spacing: .01em;
            flex: 1 1 0;
            padding-right: 10px;
            white-space: pre-line;
            min-width: 0;
            overflow-wrap: break-word;
        }

        .delete-button {
            background: none;
            color: var(--danger);
            border: none;
            outline: none;
            border-radius: 40px;
            padding: 6px 13px 6px 10px;
            font-size: 1.2rem;
            font-weight: 500;
            cursor: pointer;
            transition: background 0.18s, color 0.15s;
            display: flex;
            align-items: center;
        }

        .delete-button:hover, .delete-button:focus {
            background: #ffecec;
            color: #b82716;
        }

        .add-preference-section {
            width: 100%;
            background: var(--card-bg);
            box-shadow: var(--shadow);
            border-radius: 20px;
            padding: 20px 20px 16px 20px;
            display: flex;
            flex-direction: column;
            align-items: stretch;
            border: 1.2px solid #f1ece6;
        }

        .add-form {
            display: flex;
            flex-direction: row;
            gap: 10px;
            width: 100%;
            margin-bottom: 10px;
        }

        .add-input {
            border: 1.7px solid var(--border-light);
            border-radius: 13px;
            font-size: 1.06rem;
            padding: 11px 15px;
            flex: 1 1 0;
            min-width: 0;
            background: #fff;
            color: var(--secondary);
            font-weight: 500;
            letter-spacing: .01em;
            transition: border-color 0.18s;
        }

        .add-input:focus {
            outline: none;
            border-color: var(--primary);
        }

        .add-button {
            background: var(--primary);
            color: #fff;
            border: none;
            border-radius: 15px;
            padding: 0 18px;
            font-size: 1.09rem;
            font-weight: 600;
            cursor: pointer;
            transition: background 0.18s, box-shadow .13s;
            letter-spacing: .01em;
            box-shadow: 0 1.5px 4px rgba(11,54,91,.09);
            min-height: 41px;
            display: flex;
            align-items: center;
        }

        .add-button:hover, .add-button:focus {
            background: #163f5c;
        }

        .success-message {
            color: var(--success);
            margin-top: 13px;
            text-align: center;
            padding: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            border-radius: 9px;
            font-weight: 600;
        }

        .error-message {
            color: var(--danger);
            margin-top: 13px;
            text-align: center;
            padding: 10px;
            border-radius: 9px;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <%- include('partials_header.ejs') %>
    <div class="container">
        <div class="profile-header">
            <h1>Agent Preferences</h1>
        </div>
        
        <% if (successMessage) { %>
            <div class="success-message">
                <i class="fas fa-check-circle"></i>
                <%= successMessage %>
            </div>
        <% } %>

        <% if (errorMessage) { %>
            <div class="error-message">
                <%= errorMessage %>
            </div>
        <% } %>

        <div class="preferences-card">
            <ul class="preferences-list">
                <% 
                    let prefs = user_preference || null;
                    if (prefs && Array.isArray(prefs) && prefs.length > 0) {
                        for (let i = 0; i < prefs.length; ++i) {
                            let pref = prefs[i];
                            if (!pref) continue;
                %>
                        <li class="preference-bar" title="<%= pref %>">
                            <span class="preference-name"><%= pref %></span>
                            <form action="/user_profile/delete_preference" method="POST" style="margin:0;">
                                <input type="hidden" name="preference" value="<%= pref %>">
                                <button class="delete-button" type="submit" title="Delete preference">
                                    <svg width="17" height="17" viewBox="0 0 22 22" fill="none" style="transform: translateY(-1px)">
                                        <circle cx="11" cy="11" r="10" fill="none"/>
                                        <path d="M7 7 L15 15 M15 7 L7 15"
                                            stroke="#e74c3c" stroke-width="2.1" stroke-linecap="round"/>
                                    </svg>
                                </button>
                            </form>
                        </li>
                <%      }
                    } else if (prefs && typeof prefs === 'string') { %>
                        <li class="preference-bar" title="<%= prefs %>">
                            <span class="preference-name"><%= prefs %></span>
                            <form action="/user_profile/delete_preference" method="POST" style="margin:0;">
                                <input type="hidden" name="preference" value="<%= prefs %>">
                                <button class="delete-button" type="submit" title="Delete preference">
                                    <svg width="17" height="17" viewBox="0 0 22 22" fill="none" style="transform: translateY(-1px)">
                                        <circle cx="11" cy="11" r="10" fill="none"/>
                                        <path d="M7 7 L15 15 M15 7 L7 15"
                                          stroke="#e74c3c" stroke-width="2.1" stroke-linecap="round"/>
                                    </svg>
                                </button>
                            </form>
                        </li>
                <%  } else { %>
                    <li style="padding:24px 0 18px 0;text-align:center;color:#9d968b;font-size:1.1rem;">
                        No preferences found.<br><span style="font-size:0.98em">Add one below.</span>
                    </li>
                <% } %>
            </ul>
        </div>
        
        <div class="add-preference-section">
            <form class="add-form" action="/user_profile/add_preference" method="POST" autocomplete="off">
                <input 
                    class="add-input"
                    type="text" 
                    name="preference" 
                    placeholder="Add a new preference…" 
                    maxlength="80"
                    required
                    aria-label="Add preference"
                >
                <button class="add-button" type="submit">
                    <svg width="18" height="18" viewBox="0 0 20 20" fill="none" style="margin-right:6px;">
                        <rect x="9" y="4" width="2" height="12" rx="1" fill="#fff"/>
                        <rect x="4" y="9" width="12" height="2" rx="1" fill="#fff"/>
                    </svg>
                    Add
                </button>
            </form>
        </div>
    </div>
    <script src="https://kit.fontawesome.com/your-font-awesome-kit.js"></script>
</body>
</html>